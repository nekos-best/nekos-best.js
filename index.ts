// TODO Add fetchRandom(category, { min, max }?) -> Promise<...>
// TODO Add fetchMultiple(category, amount, { min, max }?) -> Promise<...>
// TODO Add fetchFile(category) -> Promise<Buffer>
// TODO Cache /endpoints for 2 hours
// TODO Add *fast* function. fetchRandom(category) -> Promise<...>

import petitio from 'petitio'

export class Client {

}

export function fetchRandom() {

}